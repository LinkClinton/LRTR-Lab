#pragma once

#include "Vector.hpp"

namespace LRTR {

	template<typename T>
	using Radius1 = Vector1<T>;

	template<typename T>
	using Radius2 = Vector2<T>;

	template<typename T>
	using Radius3 = Vector3<T>;
}
